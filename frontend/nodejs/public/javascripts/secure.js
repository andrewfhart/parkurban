/**
 * Content controllers for secure pages
 *
 **/
$(document).bind('pageinit', function() {
  /*
  if (activeTab) {
    $("div[data-role='navbar'] a#nav-"+activeTab).addClass('ui-btn-active');
  }
  */
});
